import 'package:flutter/material.dart';

class AssignedLeadSection extends StatelessWidget {
  const AssignedLeadSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
